# The order /issue next takes the open issues. Bare numbers only; # lines are notes.
# Written by /issue-plan on 2026-09-04. Попередня черга (#19–#26) закрита цілком;
# #27 і #28 змерджені й чекають на ручну перевірку (harness:verify) — вони внизу.

# --- auto: усе знімається наборами демона й Gradle, телефон лише лишається на дроті

34   # S: `hello` з валідним токеном, у якому падає запис конфіга, лишає сокет
     # автентифікованим без `hello.ok`. Рукостискання — фундамент усього іншого,
     # і виправлення мале: daemon/test/ + один тест дедлайну в app/. Іде першим,
     # щоб решта issue будувалася на з'єднанні, яке або встановлене, або закрите.

32   # S: дзвінок, якого телефон не закрив, лишається `active` назавжди й
     # перемальовує картку таймера щосекунди. Цілком у демоні (`liveCall()`,
     # `talkTime`, `handsfree.busy()`), критерії — тести зі зсунутим годинником.
     # Ніякого app/: hands-free гілка має пережити зникнення app-сокета.

35   # M: Outbox і Backlog не мають жодного тесту, і `abortOnError false` глушить
     # lint. Це калібрування контракту перед тим, як #29 почне класти в Backlog
     # ключ ідентичності — спершу межа в 30/200 записів під тестом, потім зміна.
     # `./gradlew test` ходить на host JVM, APK не потрібен.

# --- apk: JS або нативна частина змінюється, тож потрібна release-збірка на телефоні
# Спершу дві безпекові — вони змінюють модель довіри й транспорт, і на них
# спирається все, що поїде по цьому дроту далі (зокрема #37).

30   # M: плагін `withDesktopCa` вмикає cleartext на весь застосунок, а release
     # без PEM узагалі не париться. Модель довіри — фундамент; до того ж без
     # робочого release+TLS немає як прийняти #36. Критерій із логкатом на
     # телефоні знімається через adb.

36   # L: тіла файлів і аудіо диктовки йдуть по LAN відкритим текстом на дефолтній
     # конфігурації. Транспорт — фундамент для #37 (мікрофон «по зашифрованому
     # дроту»), тому до нього. Перевірка — tcpdump, sha256 і 50-МБ файл через
     # adb; диктовку харнес запускає з `am start` + `input`.

29   # L: дзеркальна подія не має стабільного ключа, тож обрив посеред
     # `phone.report` подвоює SMS. Після #35, бо ключ треба протягнути крізь
     # Backlog. Дедуп перевіряється на телефоні дзеркалом сповіщення
     # (`cmd notification post` — справжній кадр по тому самому шляху), тож
     # реального SMS для критеріїв не потрібно.

31   # L: довгий транскрипт гальмує телефон блок за блоком. Нічого спільного з
     # протоколом, тому остання з apk-групи. Прокрутка й знімок екрана — adb.

# --- human: харнес реалізує, тестує й мерджить; критерії добиває людина

33   # M: невдалий QR-паринг перезапускає сам себе з того ж кадру. Найменша з
     # трьох і ні від чого не залежить. Людина потрібна фізично: критерії
     # вимагають тримати QR у кадрі камери й натиснути «Scan again».

37   # L: живе аудіо з мікрофона телефона по зашифрованому дроту. Іде після #36.
     # Харнес знімає згаслий екран, обрив сокета й RSS сам, але «у файлі чути
     # сказане» вимагає, щоб хтось говорив у телефон — adb не підставляє звук
     # у справжній мікрофон.

38   # L: той самий звук як `module-pipe-source` у pipewire. Споживає потік із
     # #37, тому після нього. Життєвий цикл модуля харнес перевіряє фейковим
     # `pactl`, але критерій `parec` → «чути сказане» — знову жива людина.

# --- змерджені, чекають на ручну перевірку (harness:verify); next.sh їх пропускає
27
28
