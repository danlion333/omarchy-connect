# The order /issue next takes the open issues. Bare numbers only; # lines are notes.
# Written by /issue-plan on 2026-09-04. Попередня черга (#19–#25) закрита цілком.
# Три відкриті issue: одна цілком у демоні (auto), дві потребують людини —
# справжнього дзвінка й повного циклу живлення десктопа.

# --- auto: усе знімається наборами демона, телефон лише лишається на дроті
26   # M: анонімний `ended` із чужим токеном більше не карбує окремий рядок
     # `unknown` і не подвоює лічильник. daemon/src/plugins/phone.js + shell/Model.js
     # + тести; жодного коду в app/. Іде першим ще й тому, що це десктопна
     # половина того самого епізоду, що й #27: спершу десктоп перестає
     # перетворювати поганий кадр на сміття, потім телефон перестає його слати.

# --- human: реалізується й мерджиться харнесом, критерії добиває людина
27   # M: PhoneStateReceiver переживає смерть процесу — той самий токен `call`,
     # `from`/`name`/`direction` у звіті `ended`. Область — тільки
     # app/modules/omarchy-telephony. Людина потрібна для справжньої розмови:
     # `am kill` посеред дзвінка неможливо зімітувати через adb.
     # Менша за #28 і в одній області, тому перша з двох.
28   # M: демон на старті заявляє про себе широкомовленням, телефон негайно
     # передзвонює. daemon/src + нативний UDP-слухач + app/src/api + PROTOCOL.md.
     # Частину (`systemctl --user restart` зі сплячим телефоном) харнес знімає
     # сам через adb, але критерій «повний цикл живлення десктопа» переживе
     # тільки людина — сесія харнеса гине разом із машиною.
