# The order /issue next takes the open issues. Bare numbers only; # lines are notes.
# Written by /issue-plan on 2026-09-04. Попередня черга (#1–#18) закрита цілком.
# Усі сім відкритих issue міняють JS або нативну частину застосунку, тож клас
# один — harness:apk: реліз-APK на телефоні, критерії знімаються через adb.

# --- фундамент: спільна конвенція, на якій пишеться решта UI
22   # один читабельний спосіб показати помилку — error boundary + нормалізатор
     # + спільний компонент на семи екранах. Іде першим: доки #21 не полагоджено,
     # у нього є живий репродюсер (нативний стек із .torrent), і всі наступні
     # екранні роботи одразу лягають на готову поверхню замість власного setError.

# --- малі та середні поодинокі роботи
21   # S: ім'я файлу з [ ] # ? кидає IllegalArgumentException замість відкриття.
     # Після #22 його повідомлення про відмову пишеться один раз, правильно.
24   # M: нотифікація «агент чекає» піднімається і зникає за 2 секунди.
     # Незалежний від #23, але той самий район (plugins/agents.js + alerts.ts) —
     # робиться до нього, бо менший і псує саме той канал, заради якого екран є.

# --- буфер обміну: ланцюг, друге стоїть на першому
19   # M: бінарна подія буфера піднімає нотифікацію з прев'ю та кнопкою Save
20   # M–L: кнопка Copy кладе саму картинку в буфер телефона; без #19 їй нема де жити

# --- воркери: найбільший блок, тому останній; #25 стоїть на #23
23   # L: список воркерів сесії, їхній стан і транскрипт (read-only)
25   # M: відповісти воркеру через композер батьківської сесії; вимагає #23
