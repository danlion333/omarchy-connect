# The order /issue next takes the open issues. Bare numbers only; # lines are notes.
# Written by /issue-plan on 2026-09-03.

# --- foundations: protocol, transport, security. Features are written once, on top.
7    # daemon survives a malformed upload name — small, calibrates the contract
3    # app rejects unencrypted text frames
4    # relocate persists an address only after hello.ok
6    # upload/download by one-shot ticket, token out of URLs
8    # localhost-only routes behind a shared secret
12   # argument hygiene (--), bond agent only says yes to its own device
5    # CLI config writes go through the daemon / merge with disk
9    # stale socket handlers, chat re-open on reconnect

# --- app features: release APK on the phone, exercised over adb
16   # pause stats subscription when nothing watches
10   # split ConnectionContext
17   # clipboard history
18   # clipboard images desktop -> phone
11   # SMS backlog off Auto Backup, bounded JS queue
15   # share-sheet target

# --- needs a person: a real call, a real SMS, a real drag onto the bar
2    # dictated message with a photo — dictation is a voice
1    # stale notifications replay — needs real events on the phone
14   # drop a file onto the bar icon — a real drag in Hyprland
13   # reply to an SMS from the panel — sends from the user's number
