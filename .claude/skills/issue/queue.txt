# The order /issue next takes the open issues. Bare numbers only; # lines are notes.
# Переписано /issue-plan на 2026-09-05. Попередня черга (#39, #40) закрита цілком;
# на її місце стали чотири нові баги: два про звук із телефона, два про дорогу
# до десктопа поза LAN.

# --- auto: демон і його суїти закривають усе; телефон лише тримає лінк

42   # M: залишок у FIFO перед `module-pipe-source` стає постійною затримкою
     # (daemon/src/lib/pipesource.js:269-283, audio.js:177-199). Зміна цілком
     # на столі — жодного рядка JS чи Kotlin, тож APK не потрібен. Затримку
     # видно між `feed()` і `parec -d omarchy_connect_phone`, критерій 3 — це
     # тест із пізнім читачем у daemon/test/audio-input.mjs. Іде першою: єдина
     # auto, і найдешевша калібровка контракту труби перед рештою.

# --- apk: змінюється JS/нативна частина, тож потрібна release-збірка

43   # M: звужений network security config довіряє лише іменам зі знімка
     # app/assets/desktop-ca.pem (withDesktopCa.js:65,103,111), у якому нема
     # tailnet-адреси. Іде поперед #44, хоч обидві apk: це фундамент моделі
     # безпеки, і саме воно ламає TLS на першій же `expo prebuild` збірці.
     # Живий критерій #44 («холодний старт у tailnet під'єднується») міряється
     # на APK, зібраному prebuild-ом, тобто на тому самому, який #43 і чинить.
     # Перевіряється через adb: `svc wifi disable`, Tailscale, `am start`,
     # журнал демона `connected from 100.x`, sha256 переданого файла.

44   # S: гонка hello ↔ `refreshEnvironment` віддає телефону endpoints без
     # тунелю (server.js:163,1482-1484,297), а `ev endpoints` у link.ts у
     # сховище не пишеться (client.ts:1239-1241). Обидва критерії-тести —
     # daemon/test/ і app/test/; живий — рестарт, `am force-stop`, Wi-Fi off,
     # холодний старт, тричі поспіль. Все через adb. Після #43, бо збірка.

# --- human: харнес реалізує, тестує й мерджить; критерії добиває людина

41   # S–M: тихий мікрофон телефона. Технічна частина знімається сама —
     # формат джерела в `pactl list sources`, daemon/test/audio.mjs,
     # audio-input.mjs, app/test/micframe.mjs, запис у docs/PROTOCOL.md.
     # Але критерії 1–3 сказані про живий голос: «та сама фраза з тієї ж
     # відстані» до і після, «гучніше без чутного кліпінгу», відношення RMS
     # мови до RMS тиші. adb не вміє ані вимовити фразу в мікрофон з тієї
     # самої відстані, ані почути кліпінг — це слухає й вимірює людина.
