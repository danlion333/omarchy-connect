# The order /issue next takes the open issues. Bare numbers only; # lines are notes.
# Переписано /issue-plan на 2026-09-04. Попередня черга (#27–#38) закрита цілком —
# лишилися дві половини однієї функції: керування мікрофоном телефону з рук людини.

# --- auto: порожньо. Жодна з відкритих issue не тримається самого лише демона.

# --- apk: JS/нативна частина застосунку змінюється, тож потрібна release-збірка

40   # M–L: картка мікрофона в застосунку плюс протокольний метод, ініційований
     # телефоном (`audio.offer` поруч із `audio.input`, за тією самою огорожею
     # `via === 'remote'`, daemon/src/plugins/audio.js:396, server.js:1404).
     # Іде першою, бо єдина з двох, яку харнес закриває сам: старт і стоп потоку
     # видно через `omarchy-connect mic status`, відмову й збереження стану після
     # перемикання вкладок — через `am start`, `input tap`, `screencap`, а
     # відхилення методу з тунелю та гонитву за `asked` — суїтом демона.
     # Зміна протоколу тут — теж підстава йти першою: #39 читає стан із
     # `status.json` і на новий метод не спирається, але порядок дешевший саме
     # такий, якщо форма `summary()` раптом зрушить.

# --- human: харнес реалізує, тестує й мерджить; критерії добиває людина

39   # M: `audio` у `status.json` плюс тумблер і рядок стану в панелі бару.
     # Демонну половину харнес знімає повністю — ключ `audio` у
     # `status --json`, форма для зупиненого демона (daemon/src/lib/state.js:150),
     # `available:false` через фейковий `pactl`. Але критерії 2, 3 і 5 сказані
     # прямо про панель: «підтверджені з панелі, без термінала», рядок стану,
     # що зникає за одне оновлення, і помилка, що потрапляє в те саме місце, що
     # й помилки агентів. Для shell/*.qml у репозиторії немає жодного суїту, і
     # adb до бару десктопа не дотягується — клацає й дивиться людина.
